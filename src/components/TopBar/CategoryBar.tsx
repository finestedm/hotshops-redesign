import { Box, Button, Chip, Stack } from "@mui/material";

export default function CategoryBar() {
    return (
        <Box>
            <Stack direction='row'>
                <Button size="small" variant="outlined" color="primary"> Smartfony </Button>
            </Stack>
        </Box>
    )
}